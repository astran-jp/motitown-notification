{
  "name": "motitown-notification",
  "private": true,
  "description": "motitown.com/notification/ のお知らせサイト（Cloudflare Worker の静的アセットとして配信）",
  "scripts": {
    "build": "node scripts/build-assets.mjs",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.137.0"
  }
}
