# 公式UI用語集(アプリ上の表記)。1行1語。「#」以降は出典メモ。
# 機能名・画面名・ボタン名・タブ名はここにある表記だけを使う。ここに無い名前を作らない。
# 迷ったら app-terms.generated.txt(アプリのソースから自動抽出した全文字列)を grep して実在を確かめる。
# 内部名(Book画面, DetailAutoplay, WordBook 等)は UI 用語ではないので書かない。

# ── アプリ・ブランド ──
モチタン
モチスピ
モチタウン
モチベア
Premium            # 課金プラン名。「プレミアム」ではなく Premium 表記
ファミリープラン

# ── 画面・タブ(下部メニュー) ──
ホーム
単語帳             # モチタンの下部メニュー(MenuBar)
フレーズ帳         # モチスピの下部メニュー(MenuBar)。内部名 Book/BookScene だが UI では「フレーズ帳」
対戦               # モチタンの下部メニュー
トーク             # モチスピの下部メニュー
マイページ
設定
学習設定           # マイページ > 設定 > 学習設定
言語設定
環境設定
自動再生           # 単語詳細の歯車アイコンで開く設定画面のセクション名(DetailAutoplaySectionTitle)
単語詳細
フレーズ詳細
フレーズ帳詳細     # フレーズ帳をタップして開く詳細(PopupBookDetail)
単語帳詳細
プレイ設定         # 対戦/冒険を始める前のポップアップ(PopupPhraseBookPlay)
モチタウンのいま

# ── 単語帳/フレーズ帳画面のセクション名(BookScene) ──
お気に入り
最近見た
My単語帳/フレーズ帳
Myフレーズ帳/単語帳
AI単語帳
AIフレーズ帳
正答率別単語帳
正答率別フレーズ帳
モチベアのオススメ単語帳
モチベアのオススメフレーズ帳
すべての単語/フレーズ
すべてのフレーズ/単語
トークで覚えたフレーズ

# ── ボタン・ラベル ──
対戦/冒険をプレイ  # フレーズ帳詳細のCTA(PopupBookDetail)
モチスピでプレイ   # モチタンでフレーズ帳詳細を開いたときのCTA
スタート
プレイボタン
冒険
再生速度（倍）     # 単語詳細/フレーズ詳細の歯車アイコンで開く「自動再生」設定の項目(DetailAutoplayPlaybackSpeed)。学習設定には無い(2026-09-09 Unity実機で確認)
歯車アイコン       # 単語詳細/フレーズ詳細右上の自動再生設定ボタン(DetailAutoplaySettingsButton)。お知らせ16でもこの表記
ピンインを表示
ハングルの読み方を表示
ふりがなを表示
例文を自動で読み上げる
先生を変更
コース変更

# ── 記憶度 ──
未学習
苦手
うろ覚え
ほぼ覚えた
覚えた
忘れかけ

# ── 機能名(お知らせで使う呼び方) ──
自動再生           # 単語詳細/フレーズ詳細の自動再生(旧お知らせ16では「自動音声読み上げ機能」)
バックグラウンド再生
無料プラン
